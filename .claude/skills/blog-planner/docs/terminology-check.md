# プロダクト固有用語チェック

読者が予備知識なしで理解できる表現を使用するためのチェックリスト。

## 基本方針

**原則**: プロダクト固有の用語を避け、一般的な用語に置き換える。

**理由**:

- 読者は「ClaudeMix」というプロダクト名を知らない
- プロダクト固有の用語は読者に予備知識を要求し、離脱を招く
- 一般的な用語に置き換えることで、読者の理解を容易にする

---

## NG用語と代替案

### プロダクト名

| NG | OK | 理由 |
| :--- | :--- | :--- |
| ClaudeMix | Remix × Cloudflare Edgeのブログ | プロダクト名を避ける |
| このブログ | 筆者が運営するブログ | 文脈に応じて |
| 当ブログ | Remix × Cloudflare Edge環境 | 文脈に応じて |

**例**:

**NG**:
「ClaudeMixでは、〜を実践しています。」

**OK**:
「筆者が運営するRemix × Cloudflare Edgeのブログでは、〜を実践しています。」

---

### Claude Code固有用語

| NG | OK | 理由 |
| :--- | :--- | :--- |
| Projects | プロジェクト設定 | 一般的な用語に置き換え |
| Skills | カスタムツール / スキル機能 | 補足説明を追加 |
| .claude ディレクトリ | Claude Codeの設定ディレクトリ（.claude/） | 補足説明を追加 |
| claude-config.json | プロジェクト設定ファイル（.claude/claude-config.json） | 補足説明を追加 |
| Slash Commands | カスタムコマンド | 一般的な用語に置き換え |

**例**:

**NG**:
「Projectsを活用することで、〜できます。」

**OK**:
「Claude Codeのプロジェクト設定（.claude/claude-config.json）を活用することで、〜できます。」

---

### 内部用語

| NG | OK | 理由 |
| :--- | :--- | :--- |
| OperatorBot | オペレーター / 開発者 | 内部用語を避ける |
| 3大層アーキテクチャ | UI層・ビジネスロジック層・データ層の3層分離 | 補足説明を追加 |
| lib層 | ビジネスロジック層 | 一般的な用語に置き換え |
| data-io層 | データアクセス層 | 一般的な用語に置き換え |

**例**:

**NG**:
「lib層に副作用を含めないことで、〜できます。」

**OK**:
「ビジネスロジック層（app/lib/）に副作用を含めないことで、〜できます。」

---

## チェックプロセス

### ステップ1: NG用語の検出

記事の中から、以下のNG用語を検索してください：

- ClaudeMix
- このブログ / 当ブログ（文脈に応じて）
- Projects（Claudeの）
- Skills（Claudeの）
- .claude（補足なし）
- claude-config.json（補足なし）
- Slash Commands
- OperatorBot
- 3大層アーキテクチャ（補足なし）
- lib層 / data-io層（補足なし）

### ステップ2: 代替案の適用

NG用語が検出された場合、上記の代替案を適用してください。

### ステップ3: 補足説明の追加

一般的な用語に置き換えられない場合は、補足説明を追加してください。

**例**:

**NG**:
「.claude ディレクトリに設定ファイルを配置します。」

**OK**:
「Claude Codeの設定ディレクトリ（.claude/）に設定ファイルを配置します。」

---

## 例外

以下の場合は、プロダクト固有用語を使用しても問題ありません：

1. **固有名詞として明示的に紹介する場合**
   - 「ClaudeMixというブログを運営しています」

2. **コードスニペット内**
   - ファイルパス、ディレクトリ名等

3. **技術的な正確性のために必要な場合**
   - Claude Code Projectsの機能を紹介する際の「Projects」

**例**:

**OK（固有名詞として紹介）**:
「筆者はClaudeMixというRemix × Cloudflare Edgeのブログを運営しています。」

**OK（コードスニペット内）**:

```json
// .claude/claude-config.json
{
  "name": "ClaudeMix"
}
```

**OK（技術的な正確性）**:
「Claude Code Projectsは、プロジェクト固有の設定を.claude/claude-config.jsonファイルで管理します。」

---

## チェックリスト

記事を公開する前に、以下をチェックしてください：

- [ ] ClaudeMix → Remix × Cloudflare Edgeのブログ
- [ ] このブログ / 当ブログ → 文脈に応じて修正
- [ ] Projects → プロジェクト設定（.claude/claude-config.json）
- [ ] Skills → カスタムツール / スキル機能
- [ ] .claude → Claude Codeの設定ディレクトリ（.claude/）
- [ ] claude-config.json → プロジェクト設定ファイル（.claude/claude-config.json）
- [ ] Slash Commands → カスタムコマンド
- [ ] OperatorBot → オペレーター / 開発者
- [ ] 3大層アーキテクチャ → UI層・ビジネスロジック層・データ層の3層分離
- [ ] lib層 → ビジネスロジック層（app/lib/）
- [ ] data-io層 → データアクセス層（app/data-io/）

---

## よくある質問

### Q1: すべてのプロダクト固有用語を避ける必要がありますか？

**A**: はい、読者が予備知識なしで理解できるよう、プロダクト固有用語は避けてください。ただし、固有名詞として明示的に紹介する場合や技術的な正確性のために必要な場合は例外です。

### Q2: 「このブログ」という表現は使えませんか？

**A**: 文脈に応じて使用できます。ただし、「ClaudeMix」というプロダクト名と混同されないよう、「筆者が運営するブログ」「Remix × Cloudflare Edge環境」等の表現を推奨します。

### Q3: コードスニペット内のプロダクト固有用語もチェックが必要ですか？

**A**: いいえ、コードスニペット内のファイルパス、ディレクトリ名等は変更不要です。
